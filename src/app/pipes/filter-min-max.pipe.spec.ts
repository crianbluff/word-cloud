import { FilterMinMaxPipe } from './filter-min-max.pipe';

describe('FilterMinMaxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMinMaxPipe();
    expect(pipe).toBeTruthy();
  });
});
